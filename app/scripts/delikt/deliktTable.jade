h3 Deliktgruppen
table.table
	if view.sectionValues
		for section in view.sectionValues
			tr(data-idx=section.idx)
				td(style='background-color: rgb('+section.rgb+');')
				th=section.key
				td.fill.text-right=section ? numeral(section.value).format() : ''
